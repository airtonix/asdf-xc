name: Release

on:
  push:
    branches:
      - main

jobs:
  release-please:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: GoogleCloudPlatform/release-please-action@v2
        with:
          release-type: simple
